name: microstrategy-metadata-dashboard

services:
  dashboard:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: microstrategy-metadata-dashboard
    ports:
      - "8080:80"
    restart: unless-stopped